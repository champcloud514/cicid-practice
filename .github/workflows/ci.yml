name: Simple CI

on:
  push:
    branches: ["main"]

jobs:
  test-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say hello
        run: echo "My Second CI run!"
        
      - name: List files in repo
        run: ls -l
